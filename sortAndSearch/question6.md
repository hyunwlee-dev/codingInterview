```
package codingInterview.sortAndSearch;

/**
 *  큰 파일 정렬:
 *  한 줄에 문자열 하나가 쓰여 있는 20GB짜리 파일이 있다고 하자.
 *  이 파일을 정렬하려면 어떻게 해야 할지 설명하라.
 *
 *  hyunwlee)
 *  hashMapList로 buket 정렬화 시키면 될까?
 *  왜 그렇게 생각한건데? 그러면 뭐가 좋은데? =>
 *  아.. 죄송합니다.
 *  당연스레 a-z까지 사전순 mapping을 생각을 했습니다.
 *
 *  Solution)
 *
 *  면접관이 20GB의 제한이 있다고 말했다면 그 말 자체가 힌트이다.
 *  이 경우에는 모든 데이터를 메모리에 올려둘 수 없다는 것을 뜻한다.
 *  그렇다면 어떻게 해야 하나?
 *
 *  => 일부분만 올려야 한다.
 *   x를 가용한 메모리의 크기라고 했을 때, 파일을 x메가바이트 단위로 쪼갤 것이다.
 *  그리고 각 파일을 개별적으로 정렬하고 정렬이 끝나면 파일 시스템에 저장할 것이다.
 *  모든 파일의 정렬이 끝나면, 하나씩 병합한다.
 *  모든 파일의 병합이 끝나 완벽히 정렬된 파일을 얻는다.
 *  이런 알고리즘을 외부 정렬(external sort)라고 부른다.
 */
public class Question6 {
}
```